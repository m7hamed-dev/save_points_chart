/// Visual template applied to charts.
enum ChartTemplateStyle {
  /// Navy dashboard cards with title, legend, grid, and tooltips.
  dashboard,

  /// Raw config/theme only — no template chrome.
  plain,
}
